require("dressing").setup({
    input = {
        enabled = true,

        -- Can be 'left', 'right', or 'center'
        title_pos = "center",

        win_options = {
            winhighlight = 'NormalFloat:DiagnosticError'
        }
    },
})
